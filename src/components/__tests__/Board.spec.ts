import { describe, it, expect } from "vitest";

import { mount } from "@vue/test-utils";
import Board from "../Board.vue";

describe("Board", () => {
  it("renders properly", () => {
    // const wrapper = mount(Board, { props: { msg: "Hello Vitest" } });
    // expect(wrapper.text()).toContain("Hello Vitest");
  });
});
